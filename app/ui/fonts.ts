import {Inter} from 'next/font/google';
import { Lusitana as Lusitania } from 'next/font/google';

export const inter = Inter({subsets: ['latin']});
export const lusitania = Lusitania({subsets: ['latin'], weight: ['400','700']});
